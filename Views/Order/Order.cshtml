@model List<Product>

<h2>Замовлені товари</h2>

@for (var i = 0; i < Model.Count; i++)
{
    <div class="form-group">
        <h3>@Model[i].Name</h3>
        <h5>Замовлено стільки: @Model[i].Quantity</h5>
    </div>
}
